Scenario: An external contractor has accessed the internal forum via the Guest WiFi and they appear to have stolen credentials for the administrative user! We have attached some logs from the forum and a full database dump in sqlite3 format to help you in your investigation.

1. What was the username of the external contractor?  
```******1```

2. What IP address did the contractor use to create their account?  
```X.X.X.X```

3. What is the post_id of the malicious post that the contractor made?  
```*```

4. What is the full URI that the credential stealer sends its data to?  
```*****//**.**.*.**/******.**p```

5. When did the contractor log into the forum as the administrator?   (UTC)  
```DD/MM/YYYY HH:mm:ss```

6. In the forum there are plaintext credentials for the LDAP connection, what is the password?  
```********1```

7. What is the user agent of the Administrator user?  
```*******/*.* (*********; ***** *** ** * **_**_*) ***********/***.** (*****, **** *****) ******/***.*.*.* ******/***.*6```

8. What time did the contractor add themselves to the Administrator group?   (UTC)  
```DD/MM/YYYY HH:mm:ss```

9. What time did the contractor download the database backup?   (UTC)  
```DD/MM/YYYY HH:mm:ss```

10. What was the size in bytes of the database backup as stated by access.log?  
```****7```





------------



Scénario :
Un prestataire externe a accédé au forum interne via le WiFi invité, et il semble qu’il ait volé les identifiants de l’utilisateur administrateur !
Nous avons joint certains journaux du forum ainsi qu’un dump complet de la base de données au format sqlite3 pour vous aider dans votre enquête.

Questions :

1. Quel était le nom d’utilisateur du prestataire externe ?  
```******1```

2. Quelle adresse IP le prestataire a-t-il utilisée pour créer son compte ?  
```X.X.X.X```

3. Quel est le post_id du message malveillant publié par le prestataire ?  
```*```

4. Quelle est l’URI complète vers laquelle l’outil de vol d’identifiants envoie ses données ?  
```*****//**.**.*.**/******.**p```

5. Quand (en UTC) le prestataire s’est-il connecté au forum en tant qu’administrateur ?  
```DD/MM/YYYY HH:mm:ss```

6. Dans le forum, il existe des identifiants en clair pour la connexion LDAP — quel est le mot de passe ?  
```********1```

7. Quel est le User-Agent de l’utilisateur administrateur ?  
```*******/*.* (*********; ***** *** ** * **_**_*) ***********/***.** (*****, **** *****) ******/***.*.*.* ******/***.*6```

8. À quelle heure (en UTC) le prestataire s’est-il ajouté au groupe des administrateurs ?  
```DD/MM/YYYY HH:mm:ss```

9. À quelle heure (en UTC) le prestataire a-t-il téléchargé la sauvegarde de la base de données ?  
```DD/MM/YYYY HH:mm:ss```

10. Quelle était la taille en octets de la sauvegarde de la base de données selon le fichier access.log ?  
```****7```
